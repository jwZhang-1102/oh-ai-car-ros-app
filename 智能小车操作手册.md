# 智能小车操作指令

> 汇总 **Windows 本地（PC）** 与 **Jetson 小车** 上操控、巡检、导航相关命令。  
> 默认小车 IP：`10.147.13.194`  
> Jetson 工作目录：`~/Rosmaster-App/rosmaster/`  
> PC 项目目录：`D:\oh-ai-car-ros-app`

---

## 一、端口与资源（必记）

| 端口 | 用途 | 依赖服务 |
|------|------|----------|
| **6000** | TCP 遥控（App / G29） | Jetson `ros/run` |
| **6500** | HTTP 视频 MJPEG | 摄像头服务 |
| **6700** | 巡检 HTTP（事件/截图） | `patrol_server.py` |

```bash
# Jetson 检查端口
ss -tlnp | grep -E '6000|6500|6700'
curl -sf http://127.0.0.1:6700/health
curl -sf http://127.0.0.1:6500/index2 -I
```

---

## 二、场景总览与互斥（答辩主线）

故事线可概括为：**场区异常处置 = 自主巡防（导航+巡检） + 多模式人工控车（手势 / 方向盘 / App，需切模式） + 展示互动（音乐伴舞）**。

> **重要（答辩勿夸大）：** 当前 **没有**「导航不停机的无缝接管」。  
> App / 方向盘控车必须：**停 Docker 导航容器（或至少停 n1）→ 再 `ros/run`**。  
> 手势在 **容器内、n1 仍在** 时可用，属 Docker 模式下的体感控车，同样不是「接管状态机」。

### 2.1 能力一览

| 能力 | 怎么开 | App / 效果 |
|------|--------|------------|
| **一边导航一边巡检** | 宿主机 `start_patrol_host.sh --bg` + 容器 **n1→n2→n3** | 检出瓶子报警、短暂停，绕过后去目标点；App **巡检模式**看历史告警 |
| **手势控车** | 容器内（n1 运行中）`python3 /root/gesture_control_ros2.py` | Docker 模式体感控车（走 `/cmd_vel`） |
| **方向盘 / App 遥控** | **先停容器**，再 `ros/run`（6000/6500） | 切到遥控模式后才能用 |
| **音乐伴舞** | 宿主机 `python3 music.py`（独占串口） | 放歌 + 动作 |
| **App 巡检** | 仅需 **6700** | 历史异常图、时间、置信度（导航场也可看） |

### 2.2 模式切换（答辩如实说）

| 从… | 到… | 操作 |
|-----|-----|------|
| 导航+巡检 | App / G29 遥控 | `docker stop`（或停 n1）→ `ros/run` → 再开 App/方向盘 |
| App / G29 遥控 | 再导航 | Ctrl+C 停 `run` → 再起容器 n1n2n3 + 可选 `--bg` |
| 导航中 | 手势 | 容器仍开；建议取消 Goal 后手势，避免与 n3 抢 `/cmd_vel` |

### 2.3 资源互斥（必记）

| ❌ 不要同时 | 原因 |
|-------------|------|
| `ros/run`（App/G29） + Docker **n1** | 抢底盘串口（所以必须先停容器） |
| **6500 视频** + YOLO `patrol_detector` | 抢 USB 摄像头 |
| **`music.py`** + `ros/run` / Docker n1 | 抢串口 |
| App 与 G29 **同时**打方向 | 共用 TCP 6000 |

| ✅ 推荐组合 | 说明 |
|-------------|------|
| **n1+n2+n3 + `start_patrol_host.sh --bg`** | 主场景：导航+巡检 |
| 导航阶段 App **只开巡检模式（6700）** | 看告警，**不要**连 6000 |
| 手势：容器 n1 在、取消 Goal 后演示 | 仍属 Docker 模式 |
| App/G29：停容器后再 `ros/run` | **模式切换**，不是热接管 |
| 音乐伴舞：单独一场 | 停导航与 `run` |

### 2.4 答辩推荐出场顺序

```text
① 自主巡防（主戏）
   巡检 --bg + n1/n2/n3 → 设目标 → 瓶子报警短停 → 绕行到点
   → App 巡检模式刷历史告警（此时仍可保持导航/巡检后台）

② 模式切换后的人工控车（任选，须先停 Docker）
   停容器 → ros/run → App 遥控/驾驶  或  G29 方向盘

③（可选）Docker 仍开启时的手势控车
   取消 Goal → gesture_control_ros2.py

④ 互动彩蛋
   停导航与 run → music.py
```

口诀：**导航巡检测 Docker；手机/方向盘必须先停容器再 run；跳舞独占串口；勿称「无缝接管」。**

---

## 三、Windows 本地（PC）命令

### 3.1 罗技 G29 方向盘控车

**前置**：Jetson 已执行 `ros/run`，6000 在监听。建议 **完全退出 G HUB**。

```cmd
cd D:\oh-ai-car-ros-app\tools
pip install pygame
```

| 用途 | 命令 |
|------|------|
| 标定踏板/方向盘 | `python logitech_g29_drive.py --backend winmm --calibrate` |
| **踏板控车（推荐）** | `python logitech_g29_drive.py --backend winmm --mode pedal --ip 10.147.13.194` |
| 仅方向盘旋转 | `python logitech_g29_drive.py --backend winmm --mode rotate --max-turn 55 --ip 10.147.13.194` |
| 比例弧线行驶 | `python logitech_g29_drive.py --backend winmm --mode drive --max-speed 30 --ip 10.147.13.194` |
| 测试 TCP 旋转 | `python logitech_g29_drive.py --test-rotate --ip 10.147.13.194` |
| 打印发送帧 | 加 `--verbose` |
| 踏板不灵敏 | 加 `--pedal-threshold 0.08` |
| 列出 winmm 设备 | `python logitech_g29_drive.py --list-devices` |

**`--mode pedal` 映射（与 App 方向键一致）**

| G29（左→右） | 动作 | cmd15 |
|--------------|------|-------|
| 离合 | 后退 | dir=2 |
| 刹车 | 停止 | dir=0 |
| 油门 | 前进 | dir=1 |
| 方向盘（未踩踏板） | 左/右旋转 | dir=5/6 |

---

### 3.2 G29 模拟驾驶 + 低延迟视频

Yahboom 默认 `6500/video_feed` 容易积压旧帧（15fps 仍可能 **~3 秒延迟**）。改用仓库自带低延迟推流（**6501**）：

**Jetson**（顺序重要：**先停 `ros/run`**，再起 6501，**最后**再 `ros/run`）：

```bash
cd ~/Rosmaster-App/rosmaster
# 1) 另终端若已在跑 ros/run，先 Ctrl+C 停掉（会占摄像头）
bash stop_patrol_host.sh 2>/dev/null || true
bash stop_camera_server.sh 2>/dev/null || true
fuser -v /dev/video0    # 应无输出

# 2) 低延迟视频（6501）
bash start_low_latency_video.sh        # 流畅 320x240
# bash start_low_latency_video.sh --hd # 清晰 640x480
# bash start_low_latency_video.sh --bg # 后台

# 3) 另开终端再启控车（G29 需要 6000）
ros/run
```

若报 `Device or resource busy`：`ros/run` 未停或摄像头未释放，按上面顺序重来。

确认：

```bash
curl -I http://127.0.0.1:6501/video_feed
curl http://127.0.0.1:6501/health
```

**PC 看画面**（任选）：

```cmd
ffplay -f mpjpeg -fflags nobuffer -flags low_delay -framedrop http://10.147.13.194:6501/video_feed
```

浏览器：`http://10.147.13.194:6501/` 或 `/video_feed`

**PC G29**（与 3.1 相同）：

```cmd
cd D:\oh-ai-car-ros-app\tools
python logitech_g29_drive.py --backend winmm --mode pedal --ip 10.147.13.194
```

摄像头须 **MJPG**（勿用 YUYV 1080p，否则仅 ~3fps）：

```bash
v4l2-ctl -d /dev/video0 --set-fmt-video=width=320,height=240,pixelformat=MJPG
v4l2-ctl -d /dev/video0 --get-fmt-video   # 应显示 MJPG
```

**画质 / 延迟调参**（默认 320×240 q=60，偏糊但延迟低；你测得 ~1s 可接受后可上调）：

| 档位 | 分辨率 | quality | 预期 |
|------|--------|---------|------|
| 默认（流畅） | 320×240 | 60 | 延迟最低，画质一般 |
| **推荐平衡** | **640×480** | **70** | 清晰不少，延迟略增 |
| 高清 | 640×480 | 80 | 更清晰，延迟/带宽再增 |

手动启动（先 **Ctrl+C 停掉** 正在跑的 6501 和 `ros/run`，确认 `fuser /dev/video0` 无输出）：

```bash
pkill -f low_latency_mjpeg.py 2>/dev/null || true
fuser -v /dev/video0    # 应无输出
bash start_low_latency_video.sh --hd   # 推荐：一键 640x480
```

PC 全屏：

```cmd
ffplay -fs -f mpjpeg -fflags nobuffer -flags low_delay -framedrop http://10.147.13.194:6501/video_feed
```

**规律**：分辨率影响最大，quality 次之；用挥手法测，延迟仍 &lt;1.5s 即可用。

上传新脚本：

```cmd
scp jetson/low_latency_mjpeg.py jetson/start_low_latency_video.sh jetson@10.147.13.194:~/Rosmaster-App/rosmaster/
```

---

### 3.3 上传脚本到 Jetson

```cmd
cd /d D:\oh-ai-car-ros-app
scp jetson/start_nav_docker.sh jetson/sync_nav_map_to_docker.sh jetson/low_latency_mjpeg.py jetson/start_low_latency_video.sh jetson/patrol/*.py jetson/patrol/*.sh jetson/patrol/danger_zones.json jetson@10.147.13.194:~/Rosmaster-App/rosmaster/
```

上传后 Jetson 去 CRLF（若报 `$'\r'` 错误）：

```bash
cd ~/Rosmaster-App/rosmaster
sed -i 's/\r$//' start_nav_docker.sh sync_nav_map_to_docker.sh start_low_latency_video.sh start_patrol_host.sh stop_patrol_host.sh verify_danger_link.sh check_nav_parallel.sh verify_parallel_ab.sh
```

---

### 3.4 Git（本地仓库）

```cmd
cd D:\oh-ai-car-ros-app
git status
git add .
git commit -m "说明"
git push origin master
ssh -T git@github.com
```

---

## 四、Jetson 宿主机命令

> 以下命令在 **Jetson SSH 终端**执行，非 Docker 容器内（除非注明）。

### 4.1 App / G29 遥控模式

```bash
# 启动 TCP 6000 控车服务（App、G29 必需）
ros/run

# 确认 6000
ss -tlnp | grep 6000

# 停止：在 ros/run 窗口 Ctrl+C
```

摄像头视频（6500，与 YOLO 互斥）：

```bash
# 若有启动脚本（按 Yahboom 环境）
# 停止摄像头（巡检前释放 camera）
bash stop_camera_server.sh
```

---

### 4.2 智能巡检（6700 + YOLO）

```bash
cd ~/Rosmaster-App/rosmaster

# 一键启动（默认：导航并行安全 + on-demand 危险区 + 后台 nav-lite）
bash start_patrol_host.sh --bg

# 前台运行（终端看 [ALERT]）
bash start_patrol_host.sh

# 接显示器看检测框（占 GPU，导航并行时慎用）
bash start_patrol_host.sh --display

# 导航仍卡顿时
bash start_patrol_host.sh --nav-lite --bg

# 单机巡检要串口蜂鸣（勿与 Docker n1 同时）
bash start_patrol_host.sh --buzzer-serial

# 停止巡检
bash stop_patrol_host.sh
```

**手动启动（等价）**

```bash
cd ~/Rosmaster-App/rosmaster
pkill -f patrol_detector
bash stop_camera_server.sh 2>/dev/null || true
nohup python3 patrol_server.py > patrol_server.log 2>&1 &
nohup python3 -u patrol_detector.py --docker-nav --pose-on-demand --nav-lite --verbose >> patrol_detector.log 2>&1 &
```

**验证**

```bash
curl http://127.0.0.1:6700/health
curl http://127.0.0.1:6700/events
tail -f patrol_detector.log
tail -f events.jsonl
ls capture/patrol/
```

---

### 4.3 Docker 自主导航（一键）

**使用宿主机最新地图（如 7 月 7 日 `src/maps/yahboomcar.*`）**

n3 读的是容器内 `install/.../maps/yahboomcar.yaml`，与宿主机 `src` 不同步。启动 n1/n2/n3 **之前**在宿主机执行：

```bash
cd ~/Rosmaster-App/rosmaster
bash sync_nav_map_to_docker.sh
```

源路径默认：`/home/jetson/code/yahboomcar_ws/src/yahboomcar_nav/maps/yahboomcar.pgm`  
可改：`HOST_MAP_DIR=/你的路径 bash sync_nav_map_to_docker.sh`

同步后：**n1 → n3 → n2**，RViz 必须重新 **2D Pose Estimate**。

```bash
cd ~/Rosmaster-App/rosmaster

# 一键：docker start + 开 3 个终端 n1 → n2 → n3
bash start_nav_docker.sh

# 三个终端同时开（需自行确认 n1 就绪）
bash start_nav_docker.sh --nowait

# 无桌面 SSH 用 tmux
bash start_nav_docker.sh --tmux
tmux attach -t rosmaster_nav

# 指定容器 ID 前缀
NAV_DOCKER_CID=2169 bash start_nav_docker.sh
```

**RViz 操作（容器 n2 窗口）**

1. **2D Pose Estimate** — 设初始位姿（必须先做）
2. **2D Goal Pose** — 设目标点，小车自主走

---

### 4.4 Docker 容器快捷指令（宿主机 ~/.bashrc）

> 容器 ID 以你机器为准（手册示例 `2169`），先 `docker ps -a` 查看。

| 快捷指令 | 完整含义 |
|----------|----------|
| `s` | `docker start <容器ID>` 启动容器 |
| `t` | `docker stop <容器ID>` 停止容器 |
| `d` | `docker exec -it <容器ID> /bin/bash` 进入容器 |

```bash
docker ps -a
docker ps
source ~/.bashrc   # 使 s/t/d 生效
```

---

### 4.5 诊断 / 自检

```bash
cd ~/Rosmaster-App/rosmaster

# 危险区 + 位姿 + 蜂鸣自检
bash verify_danger_link.sh

# 导航 + 巡检并行诊断
bash check_nav_parallel.sh
bash verify_parallel_ab.sh

# 串口是否被占（并行导航时应为空）
fuser /dev/myserial 2>/dev/null || echo "串口空闲"

# 停巡检后再测导航
pkill -f patrol_detector
pkill -f patrol_server
```

---

## 五、Docker 容器内命令

> 先 `d` 进入容器。以下 `n1/n2/...` 为容器内 `~/.bashrc` 快捷指令（autodrive 镜像）。

### 5.1 自主导航（手册 3.7）

| 快捷指令 | 完整命令 | 用途 |
|----------|----------|------|
| **n1** | `ros2 launch yahboomcar_nav laser_bringup_launch.py` | 导航基础（雷达+底盘） |
| **n2** | `ros2 launch yahboomcar_nav display_nav_launch.py` | RViz 展示 |
| **n3** | `ros2 launch yahboomcar_nav navigation_dwa_launch.py` | DWA 导航 |
| **n4** | `ros2 launch yahboomcar_nav navigation_teb_launch.py` | TEB 导航（可选） |

**推荐顺序**：`n1` → 等 15s → `n2` → `n3` → RViz 设位姿与目标点

**容器内诊断（Foxy，无 `--once`）**

```bash
ros2 topic hz /scan --window 5
ros2 topic hz /odom --window 5
timeout 5 ros2 topic echo /amcl_pose 2>&1 | head -20
timeout 8 ros2 run tf2_ros tf2_echo map base_footprint
timeout 5 ros2 topic hz /cmd_vel --window 5
ros2 lifecycle get /map_server
ros2 lifecycle get /amcl
```

---

### 5.2 建图（手册 3.7，可选）

| 快捷指令 | 用途 |
|----------|------|
| **m1** | 启动 gmapping 建图基础 |
| **m2** | 建图 RViz 展示 |
| **m3** | 键盘控制小车建图 |
| **m4** | 保存地图 |

顺序：`m1` → `m2` → `m3` 建图 → Ctrl+C 停 m3 → `m4` 保存

---

## 六、OpenHarmony App 操作

> 无终端命令，按 App 内操作。

| 场景 | App 操作 |
|------|----------|
| 遥控 + 视频 | 网络页填 IP，勾选 **TCP + 视频** → 保存并进入 |
| 仅巡检 | **保存并进入（巡检模式）** → 智能巡检 → 自动刷新 |
| 遥控页 | 摇杆（cmd10）、方向按钮（cmd15）、麦轮页（cmd21） |
| 默认 IP | `10.147.13.194`（网络配置页可改） |

---

## 七、答辩推荐流程（复制即用）

> 主戏 **流程 A**。App/G29 是 **停容器后的模式切换**，不是热接管。

### 流程 A：一边导航一边巡检（主场景）

```bash
cd ~/Rosmaster-App/rosmaster
bash start_patrol_host.sh --bg
curl -sf http://127.0.0.1:6700/health
bash start_nav_docker.sh
# RViz：2D Pose Estimate → 2D Goal Pose
# 瓶子 → 报警短停 → 绕行到终点
```

App：**巡检模式** → 智能巡检（只要 6700）。此阶段不要 `ros/run`、不要 App 连 6000。

---

### 流程 B：手势控车（Docker 仍开）

```bash
# n1 在跑；建议先取消 Goal，避免与 n3 抢 cmd_vel
docker exec -it <容器ID> bash
python3 /root/gesture_control_ros2.py
```

---

### 流程 C：切到 App 遥控 / 驾驶（必须先停容器）

```bash
# 1) 停导航容器（示例）
docker stop <容器ID>
# 2) 若 YOLO 占摄像头、还要 App 看 6500 直播：先 bash stop_patrol_host.sh
# 3) 起遥控
ros/run
ss -tlnp | grep -E '6000|6500|6700'
```

App：TCP + 视频 → 方向键；或 **驾驶** 横屏直播。历史告警仍走 6700（若 app2/patrol_server 还在）。

---

### 流程 D：切到 G29（同样必须先停容器）

```bash
docker stop <容器ID>
ros/run
ss -tlnp | grep 6000
```

```cmd
cd D:\oh-ai-car-ros-app\tools
python logitech_g29_drive.py --backend pygame --mode pedal --ip 10.147.13.194
REM 仅测 TCP：python logitech_g29_drive.py --test-rotate --ip 10.147.13.194
```

---

### 流程 E：音乐伴舞

```bash
# 停 ros/run、停 Docker、停占串口进程
amixer -c 0 set PCM 90% unmute
python3 music.py
```

---

## 八、相关文档

| 文档 | 内容 |
|------|------|
| [readme.md](./readme.md) | 项目总览 |
| [doc/ros_api.md](./doc/ros_api.md) | TCP 6000 协议 |
| [tools/README.md](./tools/README.md) | G29 详细说明 |
| [jetson/patrol/INTEGRATION.md](./jetson/patrol/INTEGRATION.md) | 导航+巡检联调 |
| [智能小车使用手册.md](./智能小车使用手册.md) | Yahboom 原厂 Docker/建图 |

---

*最后更新：App/G29 为「停容器 + ros/run」模式切换，不作无缝接管表述；见第二节。*
